{
  "name": "monero-mock-pool",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal mock Monero stratum pool for zero-config local demos and tests",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
