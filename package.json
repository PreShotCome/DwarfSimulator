{
  "name": "monero-mining-app",
  "version": "0.1.0",
  "private": true,
  "description": "Monero RandomX CPU miner with a live monitoring dashboard",
  "scripts": {
    "install:all": "npm --prefix mock-pool install && npm --prefix server install && npm --prefix dashboard install",
    "dev:pool": "npm --prefix mock-pool run dev",
    "dev:server": "npm --prefix server run dev",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "dev": "concurrently -n pool,server,dashboard -c yellow,green,cyan \"npm:dev:pool\" \"npm:dev:server\" \"npm:dev:dashboard\"",
    "build": "npm --prefix mock-pool run build && npm --prefix server run build && npm --prefix dashboard run build",
    "test": "npm --prefix server test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
